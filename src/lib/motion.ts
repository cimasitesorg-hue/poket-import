/**
 * Curvas de easing para Motion (espejo de los tokens CSS de src/index.css).
 * Motion no lee CSS vars en `ease`, por eso viven acá una sola vez.
 */
export const EASE_OUT_EXPO = [0.16, 1, 0.3, 1] as const; // --ease-out-expo
export const EASE_DRAWER = [0.32, 0.72, 0, 1] as const; // --ease-drawer
